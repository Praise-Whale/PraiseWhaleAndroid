package com.sopt27.praisewhale

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

class CircularActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.fragment_praise_level)
        //val circularProgressBar = findViewById<CircularProgressBar>(R.id.cpb_circlebar)


        // Set Init progress with animation
    }
}